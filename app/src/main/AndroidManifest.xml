<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.doaa">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Doaa">
        <activity android:name=".test.TestAppActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".test.QuranTextActivity"></activity>
        <activity android:name=".test.QuranImageActivity">

        </activity>
        <activity android:name=".display.DisplayQuranActivity" />
        <activity android:name=".display.DisplayDuaActivity" />
        <activity android:name=".DonationsAndCharitiesActivity" />
        <activity android:name=".ContactUsActivity" />
        <activity android:name=".visit_graves.VisitTheGravesActivity" />
        <activity android:name=".second_activity.HomeActivity" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>